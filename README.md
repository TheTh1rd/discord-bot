# DiscordBot
 Friendly Neighborhood Bot Ralph

# Running the bot
From the root of the project install the node modules:

```bash
npm install
```

Initialize DataBase:

```bash
node dbInit.js
```

To run the bot:

```bash
node index.js
```
